name: Docker Image CI

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
        name: Checkout
      - name: Docker build
        run: |
          docker build -t love:latest .
